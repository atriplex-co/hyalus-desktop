version: "3"
services:
  web:
    build:
      dockerfile: "Dockerfile.dev"
      context: "."
    restart: always
    ports:
      - ${PORT}:3000
    volumes:
      - ".:/home/app/app"
  db:
    image: mongo:latest
    restart: always
    ports:
      - 127.0.0.1:27017:27017
    volumes:
      - db:/data/db
volumes:
  db:
