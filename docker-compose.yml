version: "3"
services:
  mongo:
    image: mongo
    restart: always
    volumes:
      - db:/data/db
  node:
    build: .
    restart: always
    ports:
      - 8080:8080
    environment:
      - MONGO=mongodb://mongo
    depends_on:
      - mongo
volumes:
  db: